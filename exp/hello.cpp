#include <bits/stdc++.h>

using namespace std;

long long power(long long a, long long b) {
  long long tmp = 1;
  for (int i = 1; i <= b; ++i) tmp = tmp * a;
}

int main() {
  // input
  ifstream in("mod.txt");
  cin.rdbuf(in.rdbuf());

  // body
  long long a, b;
  cin >> a >> b;

  return 0;
}